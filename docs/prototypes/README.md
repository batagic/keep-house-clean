# Prototypes — không deploy

HTML thử layout / phiên bản UI cũ (v2, v3, v3.1, …). **Không** được GitHub Pages hay API production sử dụng.

Trang chạy thật nằm trong [`code/`](../code/) (`index.html`, `nhat-ky.html`, …).

Mở local để tham khảo: `python3 -m http.server` trong thư mục này hoặc mở file trực tiếp trình duyệt.
